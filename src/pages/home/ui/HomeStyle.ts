import { StyleSheet } from "react-native";

const HomeStyle = StyleSheet.create({
    pageContainer: {
        flex: 1,
        alignItems: "center",
    },

    pageTitle: {
        fontWeight: 600,
    },
});

export default HomeStyle;